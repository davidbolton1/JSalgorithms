function largestOfFour(arr) {
    var empty = [];
    var maxLength = arr.length;
    for(var i = 0; i < arr.length i++){
    if(empty[i].length > MaxLength)
        maxLength = empty[i].length
    }
    return empty;
  }
  