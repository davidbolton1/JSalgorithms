occuranceCounter = (x, y, v) => {
	// Define empty array to store range of values
	let dataset = [1, 2, 3, 4];
	// Define a start and end of range
	const start = x;
    const end = Math.pow(y, 2);
	// Define number to search for inside of range
	const search = v;
    // range = () => {return dataset = Array.from({ length: end - start }, (v, k) => k + start)};
    // range()
    // console.log(dataset)

	// let count = dataset.reduce((n, val) => n + (val === search), 0);
    // console.log(count)

    
   
};


occuranceCounter(4, 4, 3);
